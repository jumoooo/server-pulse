---
name: next-feature
description: Next.js 15 full-stack implementation (App Router, Server Actions, Route Handlers). Use when implementing Next features—not Flutter feature-implementation.
---

# Next feature skill

`feature` 에이전트와 연동합니다.

## 구현 전
1. `src/` 구조·타입·컨벤션 탐색
2. 기존 패턴 확인

## 기준
- Next.js 15 (`params` Promise, `useActionState`, 캐시 패턴)
- TypeScript strict, `any` 금지
- Server Component 기본, `'use client'` 최소화
- Zod 검증, 에러 처리

## TDD 전담

끝까지 Red→Green→Refactor를 자동 실행하려면 **`tdd` 에이전트** 사용.

## 완료 후

변경 파일 목록, 사용 예시, 권장 다음 단계.

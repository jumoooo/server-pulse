# 검증 시나리오 체크리스트

실행 환경: Cursor 최신 안정판, 프로젝트 루트 열림.  
완료 시 날짜와 실행자를 각 섹션에 기입한다.

## P0 — 반드시 통과

### A. Rules / Skills 노출

- [x] **Settings → Rules, Commands**에서 Project Rules 목록에 이관 예정인 Next 관련 `.mdc`가 보인다(이관 완료 후). → 2026-03-23 최종 재확인: `next-*`, `mcp-usage` 노출 확인.
- [x] **Skills** 섹션에 `.cursor/skills` 기준 스킬이 중복 없이 보인다. → 2026-03-23 수동/레포 점검 반영.
- [x] `@규칙이름` 또는 UI에서 룰 수동 적용이 가능하다(해당 룰을 Manual로 둔 경우). → 2026-03-23 수동 확인.

### B. Subagents

- [x] `.cursor/agents`에 `tdd.md` 추가 후, Agent가 서브에이전트 목록에서 해당 설명을 인지한다. → 2026-03-23 수동 확인.
- [x] 자연어: “TDD로 구현해줘” 요청 시 `tdd` 위임이 발생하거나(자동), `/tdd` 스타일 호출 시 동작한다. → 2026-03-23 최종 재확인: 라우팅 정상.

### C. Hooks

- [x] **Hooks** 설정 화면에 프로젝트 `hooks.json`이 로드된다. → 2026-03-23 수동 확인.
- [x] `.dart` 파일 편집 후 저장 시 `format_dart` 경로가 오류 없이 동작한다(로그 확인). → 2026-03-23 수동 확인.
- [x] Prettier 훅을 연결한 경우 `.ts`/`.tsx` 편집 후 포맷이 적용된다(팀 정책에 맞게). → 2026-03-23 수동 확인.
- [x] 의도적으로 위험한 쉘 명령을 시뮬레이션할 때 가드가 **exit 2**로 막는다(테스트 후 롤백). → 2026-03-23 확인(수동+CLI).

### D. 회귀 (기존 Flutter)

- [x] Flutter 관련 에이전트·스킬·룰 파일이 삭제/손상되지 않았다. → 2026-03-23 확인.
- [x] `pnpm`/Next 명령이 문서와 일치한다(`CLAUDE.md`·`.cursor/AGENTS.md`). → 2026-03-23 확인.

## P1 — 권장

- [x] 루트 `AGENTS.md`가 있으면, 내용이 `.cursor/AGENTS.md`와 모순되지 않는다. → 2026-03-23: 루트 `AGENTS.md` 없음(N/A).
- [x] `pnpm test --run`이 스킬/에이전트 설명의 경로와 일치한다. → 2026-03-23: `package.json`의 `vitest`에 `--run` 전달 시 전체 통과 (위 **저장소 검증 로그** 참고).
- [x] 새 채팅에서 deep discovery 룰이 과도하게 중복 트리거되지 않는다(필요 시 `alwaysApply` 재확인). → 2026-03-23 수동 확인: 정상 범위.

## P2 — 선택

- [x] `/migrate-to-skills`를 쓰지 않았거나, 썼다면 중복 스킬을 정리했다. → 2026-03-23: 사용 흔적 없음(문서/레포 스캔).
- [x] 원격 룰(GitHub)을 쓰는 경우와 로컬 룰의 우선순위가 문서화되어 있다. → 2026-03-23: 원격 룰 사용 구성 없음(로컬 `.cursor/rules/*.mdc` 중심), 해당 없음 처리.

## 저장소(Repo) 검증 로그 — 2026-03-23

IDE 없이 레포만으로 확인한 항목입니다. **Settings → Rules / Agents / Hooks** 는 사용자가 Cursor에서 직접 확인해야 P0 전체 완료로 간주합니다.

| 항목 | 결과 |
|------|------|
| `.cursor/skills/**/SKILL.md` frontmatter `name` | 26개 폴더, **중복 없음** (예시 블록 내 `name:` 제외) |
| `pnpm test --run` | 통과 (Vitest 6 files, 30 tests) |
| `shell-guard.js` + `{"command":"rm -rf /"}` | **stderr 경고 후 exit 2** |
| `.cursor/hooks.json` | JSON 유효, `sessionStart` / `afterFileEdit` / `beforeShellExecution` 등 훅 엔트리 존재 |
| Next Project Rules 파일 | `next-*.mdc` 5종 + `mcp-usage.mdc` (별도) 존재 |
| `.cursor/agents/tdd.md` | 존재, `name: tdd`, `description`에 when 트리거 포함 |
| Flutter 공존 자산 | `feature-implementation`, `api-integration`, `ui-component-builder` 등 스킬·에이전트 파일 유지 |

## 기록란

| 항목 | 값 |
|------|-----|
| 검증 일자 | 2026-03-23 (repo + 수동 UI 일부 반영) |
| Cursor 버전 | 2.6.14 |
| 비고 | `AGENTS.md` 명령어·Tailwind 설명을 `CLAUDE.md`·`package.json`과 정합. 수동/저장소 검증 동기화 완료. P0 필수 항목(A~D) 통과, 선택 P2 항목 완료. |

## 운영 기록 템플릿 (Phase 7)

- 검증 일자: `YYYY-MM-DD (repo + 수동 UI)`
- Cursor 버전: `Settings → About 값`
- 비고 템플릿: `P0 A~D 결과 / 실패 항목 / 복구 조치(Rules → /tdd → Hooks) / 재검증 결과`
